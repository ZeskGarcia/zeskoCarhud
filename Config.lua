Config = {
    UseKMH = true, --Check this if you want to use KMH instead of MPH
    DEBUG = false, -- Check this if you want to get DEBUG logs
    CustomNotify = false, -- Check this if you want to add your custom notifications.
    Texts = {
        ['notInCar'] = "You are not in a car, so you can't use this command"
    }
}

CustomNotification = function(text)
    -- Add here the code for your notifications.
end
